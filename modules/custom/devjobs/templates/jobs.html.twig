{# /** * @file * Default theme implementation to display a single page. * * The
doctype, html, head and body tags are not in this template. Instead they * can
be found in the html.html.twig template in this directory. * * Available
variables: * * General utility variables: * - base_path: The base URL path of
the Drupal installation. Will usually be * "/" unless you have installed Drupal
in a sub-directory. * - is_front: A flag indicating if the current page is the
front page. * - logged_in: A flag indicating if the user is registered and
signed in. * - is_admin: A flag indicating if the user has permission to access
* administration pages. * * Site identity: * - front_page: The URL of the front
page. Use this instead of base_path when * linking to the front page. This
includes the language domain or prefix. * * Page content (in order of occurrence
in the default page.html.twig): * - messages: Status and error messages. Should
be displayed prominently. * - node: Fully loaded node, if there is an
automatically-loaded node * associated with the page and the node ID is the
second argument in the * page's path (e.g. node/12345 and node/12345/revisions,
but not * comment/reply/12345). * * Regions: * - page.header: Items for the
header region. * - page.primary_menu: Items for the primary menu region. * -
page.secondary_menu: Items for the secondary menu region. * - page.highlighted:
Items for the highlighted content region. * - page.help: Dynamic help text,
mostly for admin pages. * - page.content: The main content of the current page.
* - page.sidebar_first: Items for the first sidebar. * - page.sidebar_second:
Items for the second sidebar. * - page.footer: Items for the footer region. * -
page.breadcrumb: Items for the breadcrumb region. * * @see
template_preprocess_page() * @see html.html.twig * * @ingroup themeable */ #}
<div>
  <div class="relative font-kumbh-sans bg-lightgray dark:bg-darkblack pb-14">
    <div class="relative">
      <div class="max-w-full">
        <img
          src="modules/custom/devjobs/images/home-bg--small.png"
          class="w-full h-32 md:hidden"
        />
        <img
          src="modules/custom/devjobs/images/home-bg--medium.png"
          class="hidden md:block md:w-full md:h-40 lg:hidden"
        />
        <img
          src="modules/custom/devjobs/images/home-bg--large.png"
          class="hidden lg:block lg:w-full lg:h-40"
        />
      </div>
      <div
        class="absolute top-0 left-0 right-0 bottom-0 mt-8 px-6 h-8 max-w-screen-lg mx-auto flex justify-between md:mt-10"
      >
        <div>
          <a href="/jobs"
            ><span class="text-white font-bold text-3xl md:text-4xl"
              >devjobs</span
            ></a
          >
        </div>
        <div class="flex gap-4">
          <div class="flex items-center">
            <img src="modules/custom/devjobs/images/sun.png" />
          </div>
          <div class="flex items-center theme-switcher">
            <div
              class="theme-switcher cursor-pointer h-6 w-12 bg-white rounded-full flex items-center"
            >
              <div
                class="theme-switcher--circle rounded-full bg-lightblue h-4 w-4 mx-1 transition-all duration-300"
              ></div>
            </div>
          </div>
          <div class="flex items-center">
            <img src="modules/custom/devjobs/images/moon.png" />
          </div>
        </div>
      </div>
    </div>
    <div
      class="absolute w-full -translate-y-2/4 md:-translate-y-3/4 lg:w-275 lg:mx-auto"
    >
      <div class="px-6 md:px-4 max-w-screen-lg mx-auto">
        <form class="bg-white dark:bg-darkblue px-6 py-4 rounded-md" type="get">
          <div class="flex justify-between items-center md:justify-between">
            <div
              class="md:flex md:items-center md:gap-4 md:border-r md:border-r-slate-200 py-4"
            >
              <div class="hidden md:inline-block">
                <img src="modules/custom/devjobs/images/loop.png" />
              </div>
              <input
                type="text"
                name="title"
                id="title"
                placeholder="Filter by title..."
                class="border-none w-auto w-48 dark:bg-darkblue dark:text-white"
              />
            </div>
            <div
              class="md:flex md:items-center md:gap-4 md:border-r md:border-r-slate-200 py-4"
            >
              <div class="hidden md:inline-block">
                <img src="modules/custom/devjobs/images/location.png" />
              </div>
              <input
                type="text"
                name="location"
                id="location"
                placeholder="Filter by location..."
                class="hidden md:inline-block border-none w-auto w-48 dark:bg-darkblue dark:text-white"
              />
            </div>
            <div class="flex items-center gap-6">
              <div class="w-5 md:w-auto">
                <img
                  src="modules/custom/devjobs/images/filter-btn.png"
                  class="cursor-pointer md:hidden filter-btn"
                />
                <div class="hidden md:flex gap-4">
                  <input
                    type="checkbox"
                    name="checkbox"
                    id="full-time"
                    class="hidden md:inline-block cursor-pointer dark:bg-darkblue"
                  />
                  <label
                    for="full-time"
                    class="hidden md:inline-block font-bold text-base cursor-pointer dark:text-white"
                    >Full Time</label
                  >
                </div>
              </div>
              <div class="w-12 md:w-20">
                <button
                  type="submit"
                  name="submit"
                  value="submit"
                  class="border-0 md:hidden"
                >
                  <img
                    src="modules/custom/devjobs/images/search.png"
                    class="cursor-pointer md:hidden"
                  />
                </button>
                <button
                  class="hidden md:flex md:items-center p-4 box-border h-12 w-20 border-0 bg-lightblue text-white font-bold rounded-md text-base"
                  type="submit"
                  name="submit"
                  value="submit"
                >
                  Search
                </button>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
    <form
      class="form-modal hidden md:hidden absolute top-0 left-0 bg-black/25 z-50 h-screen w-screen flex items-center justify-center"
    >
      <div
        class="mx-6 bg-white dark:bg-darkblue w-full rounded-md modal-window"
      >
        <div class="flex flex-col">
          <div
            class="flex gap-4 border-b border-slate-200 dark:border-slate-800 p-6"
          >
            <div>
              <img src="modules/custom/devjobs/images/location.png" />
            </div>
            <input
              type="text"
              name="location"
              id="location"
              placeholder="Filter by location..."
              class="md:inline-block border-none w-auto w-48 dark:bg-darkblue dark:text-white"
            />
          </div>
          <div class="p-6">
            <input
              type="checkbox"
              name="checkbox"
              id="full-time-only"
              class="md:inline-block cursor-pointer dark:bg-darkblue"
            />
            <label
              for="full-time-only"
              class="md:inline-block font-bold text-base cursor-pointer dark:text-white ml-4"
              >Full Time Only</label
            >
          </div>
          <div class="p-6">
            <button
              class="flex items-center justify-center p-4 box-border h-12 w-full border-0 bg-lightblue text-white font-bold rounded-md text-base"
              type="submit"
              name="submit"
              value="submit"
            >
              Search
            </button>
          </div>
        </div>
      </div>
    </form>
    <div class="max-w-screen-lg mx-auto">
      <div
        class="mt-24 grid grid-cols-1 gap-y-12 px-6 md:grid-cols-2 md:gap-x-3 md:gap-y-16 lg:grid-cols-3"
      >
        {% for job in jobs %}
        <div
          class="p-8 pt-0 relative bg-white dark:bg-darkblue rounded-md flex flex-col h-64"
        >
          <img
            src="{{ job.card_image }}"
            class="absolute top-0 left-0 translate-x-2/4 -translate-y-2/4"
          />
          <div class="flex flex-col mt-12 gap-4">
            <div class="flex text-bluegray items-center gap-3">
              <span>{{ job.time_ago }}</span>
              <div class="w-1 h-1 bg-bluegray rounded-full"></div>
              <span>{{ job.job_type }}</span>
            </div>
            <a href="/node/{{ job.id }}"
              ><span
                class="font-bold text-xl text-lightblack dark:text-white"
                >{{ job.job_title }}</span
              ></a
            >
            <span class="text-bluegray">{{ job.company_name }}</span>
          </div>
          <span class="inline-block mt-auto text-lightblue font-bold text-sm">{{
            job.country
          }}</span>
        </div>
        {% endfor %}
      </div>
      <div
        class="w-36 h-11 bg-lightblue text-white font-bold mx-auto mt-8 text-center rounded flex items-center justify-center"
      >
        Load More
      </div>
    </div>
  </div>
</div>
